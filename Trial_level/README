1. From the terminal navigate to the directory which this folder is in(eg if this folder is in desktop then cd desktop)
2. Run the file run.py (python3 run.py)
3. An ouput file will be created called output{i}.csv 
