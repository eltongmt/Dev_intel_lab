1. From the terminal navigate to the directory which this folder is in(eg if this folder is in desktop then cd desktop/Trial_level)
2. Run the file run.py (python3 run.py)
3. An ouput file will be created called {measures}{i}.csv, a new directory output will also be created in Trial_level 
